# Contributing
Issues and pull requests are welcome, especially results on additional public datasets, adversarial-robustness tests, and scaling work.
1. Fork and create a feature branch.
2. `pip install -r requirements.txt && pytest -q` must pass.
3. For any new metric claim, include the command and the `reports/` output that produced it.
